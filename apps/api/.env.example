# apps/api/.env.example

# ────── Server ──────
PORT=4000
NODE_ENV=development

# ────── CORS ──────
CORS_ORIGIN=http://localhost:3000

# ────── Database (Neon) ──────
DATABASE_URL=
DIRECT_URL=

# ────── Auth ──────
JWT_SECRET=
JWT_EXPIRES_IN=7d

# ────── OpenAI ──────
OPENAI_API_KEY=
